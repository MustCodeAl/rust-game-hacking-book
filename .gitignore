.DS_Store

# Cargo creates a target directory beside each lab crate. These files are
# machine-specific build output and must never be committed.
target/

# Leftovers from the Jekyll edition, which lives on the jekyll-backup branch.
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/

# Local copies of the original course projects.
projects/
