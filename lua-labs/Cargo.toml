[package]
name = "gha-lua-labs"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1.0"
mlua = { version = "0.12", features = ["lua54", "vendored", "error-send"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
pedantic = "warn"
