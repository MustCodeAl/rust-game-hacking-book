---
// Lesson pages get the book's header: chapter kicker, title, summary, and
// reading time. Pages that draw their own heading (hideTitle) keep only the
// `_top` anchor Starlight's table of contents links to.
import { chapterOf } from '../../data/chapters.mjs';

const { title, description, chapter, minutes, hideTitle } = Astro.locals.starlightRoute.entry.data;
const info = chapter ? chapterOf(chapter) : undefined;
---

{
	hideTitle ? (
		<span id="_top" />
	) : info ? (
		<header class="lesson-header">
			<div class="lesson-kicker">
				<span>
					<span aria-hidden="true">{info.emoji}</span> Lesson {chapter}
				</span>
				<span>{info.title}</span>
			</div>
			<h1 id="_top">{title}</h1>
			{description && <p class="lesson-summary">{description}</p>}
			{
				minutes && (
					<div class="lesson-stats" aria-label="Lesson details">
						<span>About {minutes} minutes</span>
					</div>
				)
			}
		</header>
	) : (
		<h1 id="_top" class="plain-title">{title}</h1>
	)
}

<style>
	.plain-title {
		margin-top: 1rem;
		font-size: var(--sl-text-h1);
		line-height: var(--sl-line-height-headings);
		font-weight: 600;
		color: var(--sl-color-white);
	}
</style>
